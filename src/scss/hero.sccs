@use 'variables';

.hero {
  padding: 4rem 2rem;
  background-color: variables.$color-surface-container; // Ajustado
  text-align: center;
  border-bottom: 2px solid variables.$color-outline-variant;

  &-container {
    max-width: 1000px;
    margin: 0 auto;
  }

  &-badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(variables.$color-primary, 0.1);
    border-radius: variables.$border-radius-full; // Ajustado
    margin-bottom: variables.$stack-md; // Ajustado
  }

  &-title {
    font-size: 3rem;
    color: variables.$color-primary;
    margin-bottom: variables.$stack-md; // Ajustado

    &-accent {
      font-style: italic;
      color: variables.$color-on-surface; // Sustituido: color-secondary no existía en variables
    }
  }

  &-description {
    font-size: 1.25rem;
    color: variables.$color-on-surface-variant;
    margin-bottom: variables.$stack-lg; // Ajustado
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  &-actions {
    display: flex;
    justify-content: center;
    gap: variables.$stack-md; // Ajustado
  }
}